import React from 'react'
import './Numbers.scss'

function Numbers({children}) {
    return (
        <div class='content'>
        <div class='css'>{children}</div>
        <div class='css'>{children}</div>
        <div class='css'>{children}</div>
        <div class='css'>{children}</div>
        <div class='css'>{children}</div>
        <div class='css'>{children}</div>
        <div class='css'>{children}</div>
        <div class='css'>{children}</div>
        <div class='css'>{children}</div>
        <div class='css'>{children}</div>
    </div>
    )
}

export default Numbers
